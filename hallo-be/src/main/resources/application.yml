# 服务器配置
server:
  port: 8000
  servlet:
    context-path: /hallo

# 数据库配置 - H2 内存数据库
spring:
  jackson:
    # 设置时区为上海
    time-zone: Asia/Shanghai

# 日志配置
logging:
  level:
    # Spring 框架日志级别
    "[org.springframework]": DEBUG
    # 应用日志级别
    "[com.hallo]": DEBUG